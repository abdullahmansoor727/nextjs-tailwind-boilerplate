import "./globals.css";

import type { Metadata } from "next";

import RootLayout from "@/components/Home/layout/RootLayout";

export const metadata: Metadata = {
  title: "Nextjs + TypeScript + TailwindCSS",
  description: "Boilerplate for Nextjs + TypeScript + TailwindCSS",
};

export default RootLayout;
